# World Model Viewer

The model viewer should combine:

- Large viewer surface for the world model.
- Annotation overlay for findings.
- Feed panel with domain filters and severity filtering.
- Agent activity ribbon with live progress.

Clicking a finding should direct camera attention to its spatial anchor.

