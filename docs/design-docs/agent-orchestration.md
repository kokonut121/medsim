# Agent Orchestration

The orchestration contract mirrors the PRD:

- Six teams run in parallel.
- Outputs use a common finding schema.
- Consensus groups co-located findings and promotes compound severity.
- Findings are persisted before publication to live consumers.

Current implementation ships deterministic domain findings to validate feed, overlay, and report behavior.

